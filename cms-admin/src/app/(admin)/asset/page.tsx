'use client';

import { useState } from 'react';

export default function AssetPage() {
  const [asset, setAsset] = useState<any[]>([]);
  return (
    <article>
      <h2>Asset Desa</h2>
    </article>
  );
}
